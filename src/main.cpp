#include <iostream>
#include <math.h>
#include <string.h>

#include "../include/window.h"

int main(int argc, char** argv)
{
  Window window("Transformacoes Poligono", argc, argv);
  window.render();
  return (0);
}